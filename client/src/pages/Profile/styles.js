import { makeStyles } from '@material-ui/core/styles';
export const useStyles = makeStyles(theme => ({
  avatar: {
    height: '150px',
    width: '150px',
    margin: '0 auto',
  },
  shortText: {
    marginRight: '1rem',
  },
}));
